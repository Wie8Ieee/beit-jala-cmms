DATABASE_URL=
SESSION_SECRET=
NODE_ENV=development
PORT=5000
DB_POOL_MAX=20
# Keep false for an internal HTTP link. Change to true only after enabling HTTPS.
COOKIE_SECURE=false
# Enable only when the app is placed behind a trusted reverse proxy.
TRUST_PROXY=false
